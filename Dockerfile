# Используем официальный образ OpenJDK (укажите нужную версию, например, 17)
FROM openjdk:17-jdk-slim

# Указываем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем скомпилированный JAR-файл в контейнер. Убедитесь, что путь соответствует сборке (например, target/shop.jar)
COPY target/shop.jar app.jar

# Указываем порт, который будет экспонироваться (например, 8080)
EXPOSE 8080

# Команда для запуска приложения
ENTRYPOINT ["java", "-jar", "app.jar"]
